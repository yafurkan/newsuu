name: 🤖 Android CI/CD - Su Takip App

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      reason:
        description: "Manuel tetikleme nedeni (opsiyonel)"
        required: false
        default: "manual run"

env:
  FLUTTER_VERSION: '3.24.3'
  JAVA_VERSION: '17'

jobs:
  # 🔍 Kod Analizi ve Testler
  test:
    name: 🧪 Test & Analyze
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      
    - name: ☕ Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: ${{ env.JAVA_VERSION }}
        
    - name: 🐦 Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: ${{ env.FLUTTER_VERSION }}
        channel: 'stable'
        cache: true
        
    - name: 📦 Get dependencies
      run: flutter pub get
      
    - name: 🔍 Analyze code
      run: flutter analyze --no-fatal-infos
      
    - name: 🧪 Run tests
      run: flutter test test/unit_test.dart

  # 🏗️ Android Build
  build-android:
    name: 🤖 Build Android APK
    runs-on: ubuntu-latest
    needs: test
    if: github.ref == 'refs/heads/main'
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      
    - name: ☕ Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: ${{ env.JAVA_VERSION }}
        
    - name: 🐦 Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: ${{ env.FLUTTER_VERSION }}
        channel: 'stable'
        cache: true

    - name: 🔎 Setup Flutter Environment and local.properties
      shell: bash
      run: |
        set -e
        # Flutter SDK yolunu bul
        FLUTTER_BIN="$(which flutter)"
        echo "Flutter binary: $FLUTTER_BIN"
        FLUTTER_HOME="$(dirname "$(dirname "$FLUTTER_BIN")")"
        echo "Flutter SDK path: $FLUTTER_HOME"
        
        # Environment variables ayarla
        echo "FLUTTER_HOME=$FLUTTER_HOME" >> $GITHUB_ENV
        echo "FLUTTER_ROOT=$FLUTTER_HOME" >> $GITHUB_ENV
        
        # local.properties dosyasını oluştur
        cat > android/local.properties <<EOF
        sdk.dir=$ANDROID_SDK_ROOT
        flutter.sdk=$FLUTTER_HOME
        flutter.buildMode=debug
        flutter.versionName=1.0.0
        flutter.versionCode=1
        EOF
        
        echo "Created android/local.properties:"
        cat android/local.properties
        
        # Debug için dosya konumlarını göster
        echo "=== DEBUG INFO ==="
        echo "Current directory: $(pwd)"
        echo "Android directory exists: $(ls -la android/)"
        echo "Local.properties exists: $(ls -la android/local.properties)"
        echo "Gradle.properties exists: $(ls -la android/gradle.properties)"
        echo "Gradle.properties content:"
        cat android/gradle.properties
        echo "=== END DEBUG ==="
         
    - name: 📦 Get dependencies
      run: flutter pub get
      
    - name: 🔧 Setup Gradle Cache
      uses: actions/cache@v3
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-

    - name: 🔧 Create .env file for CI
      run: |
        echo "# CI Environment Variables" > .env
        echo "FLUTTER_ENV=production" >> .env
        echo "API_BASE_URL=https://api.example.com" >> .env
        
    - name: 🛠️ Configure gradle.properties for CI
      shell: bash
      run: |
        set -e
        echo "Configuring gradle.properties..."
        echo "GITHUB_WORKSPACE=$GITHUB_WORKSPACE"
        echo "FLUTTER_HOME=$FLUTTER_HOME"
        
        # gradle.properties dosyasına Flutter ayarlarını ekle
        sed -i "s#flutter.source=.*#flutter.source=..#g" android/gradle.properties
        sed -i "s#flutter.sdk=.*#flutter.sdk=$FLUTTER_HOME#g" android/gradle.properties
        
        echo "Updated android/gradle.properties:"
        cat android/gradle.properties

    - name: 🔧 Build APK
      run: |
        flutter clean
        flutter pub get
        flutter build apk --debug
      
    - name: 📤 Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: android-apk
        path: build/app/outputs/flutter-apk/app-debug.apk
        
    - name: 📊 APK Info
      run: |
        echo "## 📱 Android APK Build Başarılı! 🎉" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### 📊 Build Bilgileri:" >> $GITHUB_STEP_SUMMARY
        echo "- **Flutter Version:** ${{ env.FLUTTER_VERSION }}" >> $GITHUB_STEP_SUMMARY
        echo "- **Java Version:** ${{ env.JAVA_VERSION }}" >> $GITHUB_STEP_SUMMARY
        echo "- **Build Type:** Debug APK" >> $GITHUB_STEP_SUMMARY
        echo "- **Commit:** ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        if [ -f "build/app/outputs/flutter-apk/app-debug.apk" ]; then
          size=$(du -h build/app/outputs/flutter-apk/app-debug.apk | cut -f1)
          echo "- **APK Size:** $size" >> $GITHUB_STEP_SUMMARY
        fi
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "✅ APK başarıyla oluşturuldu ve artifacts'a yüklendi!" >> $GITHUB_STEP_SUMMARY

  # 🚀 Firebase App Distribution
  deploy-firebase:
    name: 🚀 Deploy to Firebase App Distribution
    runs-on: ubuntu-latest
    needs: build-android
    if: github.ref == 'refs/heads/main'  # Ana branch'e push olduğunda çalışır
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      
    - name: 📥 Download APK
      uses: actions/download-artifact@v4
      with:
        name: android-apk
        path: ./
        
    - name: 🚀 Deploy to Firebase App Distribution
      uses: wzieba/Firebase-Distribution-Github-Action@v1
      with:
        appId: ${{ secrets.FIREBASE_APP_ID_ANDROID }}
        serviceCredentialsFileContent: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_KEY }}
        groups: testers
        file: app-debug.apk
        releaseNotes: |
          🚀 Su Takip App - Yeni Sürüm!
          
          📝 Son Değişiklikler:
          ${{ github.event.head_commit.message }}
          
          📊 Build Bilgileri:
          • Flutter: ${{ env.FLUTTER_VERSION }}
          • Build: #${{ github.run_number }}
          • Commit: ${{ github.sha }}
          • Branch: ${{ github.ref_name }}
          
          📱 Test için uygulamayı indirin ve geri bildirimlerinizi paylaşın!
          
          🔗 GitHub: https://github.com/${{ github.repository }}/commit/${{ github.sha }}